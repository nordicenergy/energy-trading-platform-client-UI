#### Meter reading form
Example:
```
const labels = {
   meterReadingsField: 'Meter readings',
   dateField: 'Date of reading',
   commentField: 'Comment',
   submitButton: 'Submit'
};

const numberOfMeter = 12343;

<MeterReadingForm labels={labels} numberOfMeter={numberOfMeter} />
```
